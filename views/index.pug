extends layout

block content
  h1= title
  for document in messages 
    p (#{document.added.getHours()}:#{document.added.getMinutes()}) #{document.user}: #{document.text}
  a(href='/new') Create a new message